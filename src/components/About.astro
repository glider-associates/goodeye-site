<section class="about" id="about">
  <div class="container">
    <div class="about-header fade-in">
      <span class="section-label" style="color: var(--color-accent);">Philosophy</span>
      <h2 class="section-title-en" style="color: var(--color-white);">Our Vision</h2>
      <p class="section-title-ja" style="color: var(--color-text-light-gray);">私たちが目指すもの</p>
    </div>

    <div class="about-body">
      <div class="about-message fade-in">
        <p class="about-lead">
          日本を代表するチーム
      </div>

      <div class="about-values-block fade-in">
        <p class="about-values-lead">そのために、私たちが大切にする3つの行動。</p>
        <div class="about-values">
          <div class="value-item">
            <span class="value-number">01</span>
            <h3 class="value-title">職人であれ</h3>
            <p class="value-title-en">PROFESSIONAL</p>
            <p class="value-desc">
              自分の仕事に誇りを持つ。<br>
              細部への執念と想像力で、<br>
              顧客の期待を超えていく。
            </p>
          </div>
          <div class="value-item">
            <span class="value-number">02</span>
            <h3 class="value-title">独創的であれ</h3>
            <p class="value-title-en">UNIQUE</p>
            <p class="value-desc">
              奇抜さを競うのではない。<br>
              課題を徹底的に掘り下げ、<br>
              その先にしかない答えをつくる。
            </p>
          </div>
          <div class="value-item">
            <span class="value-number">03</span>
            <h3 class="value-title">最短距離</h3>
            <p class="value-title-en">SHORTEST WAY</p>
            <p class="value-desc">
              成果を妨げるものを削ぎ落とす。<br>
              本質を捉え、決めたら一直線に進む。
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    background: var(--color-bg-dark);
    padding: var(--section-padding) 0;
  }

  .about-header {
    margin-bottom: clamp(3rem, 6vw, 5rem);
  }

  .about-body {
    display: grid;
    gap: clamp(3rem, 6vw, 5rem);
  }

  .about-message {
    max-width: 700px;
  }

  .about-lead {
    font-size: clamp(1rem, 1.8vw, 1.3rem);
    font-weight: 300;
    line-height: 2.2;
    color: rgba(255, 255, 255, 0.85);
    letter-spacing: 0.06em;
  }

  .about-values-lead {
    font-size: clamp(0.9rem, 1.5vw, 1.1rem);
    font-weight: 400;
    line-height: 1.8;
    color: var(--color-white);
    letter-spacing: 0.08em;
    margin-bottom: clamp(1.5rem, 3vw, 2.5rem);
  }

  .about-values {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .value-item {
    padding: 2.5rem 2rem;
    border: 1px solid var(--color-border);
    transition: border-color 0.4s ease;
  }

  .value-item:hover {
    border-color: var(--color-accent);
  }

  .value-number {
    font-family: var(--font-en);
    font-size: clamp(1.15rem, 1.8vw, 1.4rem);
    color: var(--color-accent);
    letter-spacing: 0.1em;
    display: block;
    margin-bottom: 1.5rem;
  }

  .value-title {
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--color-white);
    margin-bottom: 0.4rem;
    letter-spacing: 0.05em;
  }

  .value-title-en {
    font-family: var(--font-en);
    font-size: 0.7rem;
    font-weight: 400;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1.2rem;
  }

  .value-desc {
    font-size: 0.8rem;
    line-height: 1.9;
    color: var(--color-text-light-gray);
    font-weight: 300;
  }

  .sp-only {
    display: none;
  }

  @media (max-width: 768px) {
    .about-values {
      grid-template-columns: 1fr;
    }

    .sp-only {
      display: inline;
    }
  }
</style>
