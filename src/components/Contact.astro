<section class="contact" id="contact">
  <div class="container">
    <div class="contact-inner fade-in">
      <div class="contact-content">
        <span class="section-label" style="color: var(--color-accent);">Get In Touch</span>
        <h2 class="contact-title">Contact Us</h2>
        <p class="contact-desc">
          ご質問・ご相談・お仕事のご依頼など、<br />
          お気軽にお問い合わせください。
        </p>
        <a href="mailto:info@good-eye.tokyo" class="btn" style="color: var(--color-text-dark);">
          <span>お問い合わせ</span>
          <span class="btn-arrow"></span>
        </a>
      </div>
      <div class="contact-deco">
        <img src="/images/restaurants/rinn/rinn-05.jpg" alt="" loading="lazy" />
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    background: var(--color-bg-light);
    padding: var(--section-padding) 0;
  }

  .contact-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .contact-title {
    font-family: var(--font-en);
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 300;
    letter-spacing: 0.02em;
    line-height: 1.1;
    color: var(--color-text-dark);
    margin-bottom: 1.5rem;
  }

  .contact-desc {
    font-size: 0.85rem;
    line-height: 2;
    color: var(--color-text-gray);
    font-weight: 300;
    margin-bottom: 2.5rem;
    letter-spacing: 0.05em;
  }

  .contact-deco {
    aspect-ratio: 4 / 3;
    overflow: hidden;
  }

  .contact-deco img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (max-width: 768px) {
    .contact-inner {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .contact-deco {
      aspect-ratio: 16 / 9;
    }
  }
</style>
